import { AuraBackground } from '@/components/aura-background';
import { FocusMode } from '@/components/focus-mode';

export default function FocusPage() {
  return <><AuraBackground /><FocusMode standalone /></>;
}
